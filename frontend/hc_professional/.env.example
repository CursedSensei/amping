# Copy this file to .env.local and fill in your values.
# .env.local is gitignored — never commit real secrets.

# Base URL for the Django backend API (no trailing slash)
VITE_API_BASE_URL=http://localhost:8000/api/v1
